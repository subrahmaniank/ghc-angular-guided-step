# step 1 - copilot instructions + manual prompt

* Copy `copilot-instructions.md` into `.github/copilot-instructions.md` in the Angular project.
* Ensure Copilot detects and uses the instructions.
* Use this manual prompt:

``Write test case for home.component.ts. Use `npm run test:coverage` to run unit tests.``
